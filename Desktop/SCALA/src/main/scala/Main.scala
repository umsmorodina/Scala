import shapeless._


object Main extends App{
  override def main(args: Array[String]) {
    val filename = "in.txt"
    val parser = new Parser
   // parser.start_parser(scala.io.Source.fromFile(filename).mkString.trim)
  }
}